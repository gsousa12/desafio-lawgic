export enum UserRole {
  Admin = 'admin',
  Notifier = 'notifier',
  Reviewer = 'reviewer',
}

export enum NotificationStatus {
  InProgress = 'in_progress',
  Validation = 'validation',
  Completed = 'completed',
}
